import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container:{
        width: '100%',
        position: 'absolute',
        top: 80,
        padding: 10,
        alignItems: 'center'
    },
    title:{
        fontSize: 18
    }
})